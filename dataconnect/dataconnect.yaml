specVersion: "v1beta"
serviceId: "novations"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "novations-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
